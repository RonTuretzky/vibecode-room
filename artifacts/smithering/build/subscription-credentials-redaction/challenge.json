{
  "ticketId": "subscription-credentials-redaction",
  "status": "ready_for_external_adversarial_challenge",
  "approvalAssertedByImplementer": false,
  "challengeFocus": [
    "raw model key construction is rejected without value echo",
    "TraceProcessor has no public default redaction opt-out",
    "embedded unknown token-shaped strings and provider-style separator tokens redact fail-closed",
    "whole-session scan covers every emitted trace/log/report tree, including build trace copies and probe reports",
    "probe report failure paths cannot leave raw token-shaped values on disk"
  ],
  "notes": "The prior rejected challenge targeted pre-fix code. External adversarial challenge must replace or amend this file with its verdict before land."
}

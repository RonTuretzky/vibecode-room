{
  "ticketId": "walking-skeleton-smoke",
  "verifier": "independent-test-authority (claude-opus-4-8)",
  "family": "anthropic",
  "branch": "build/walking-skeleton-smoke",
  "ranTests": true,
  "rbgConfirmed": true,
  "gatesGreen": true,
  "pass": true,
  "note": "Re-derived from disk in a fresh, distinct run. Current disk state: test/smoke/spine-skeleton.smoke.ts (single test, 12 expect calls); scoped `bun test ./src ./test` 42 pass/0 fail across 8 files.",
  "gates": [
    {
      "id": "smoke-spine-skeleton",
      "command": "bun test test/smoke/spine-skeleton.smoke.ts",
      "green": "1 pass / 0 fail / 12 expect() calls (exit 0)",
      "red": "BREAK_MATCHER=1 -> 0 pass / 1 fail at expect(emitted.length).toBe(1): Expected 1 Received 0 (exit 1)",
      "rbgGenuine": true,
      "note": "Both legs reproduced live from disk. RED is a real assertion failure of a real test; GREEN passes. Matches archived smoke-rbg-red.log / smoke-rbg-green.log."
    },
    {
      "id": "tsc-noEmit",
      "command": "bun run typecheck (tsc --noEmit)",
      "green": "exit 0, no diagnostics",
      "red": "appended `const _verifierRbg: number = \"string\"` to src/types.ts -> TS2322 'string not assignable to number' (exit 2); restored -> clean (exit 0)",
      "rbgGenuine": true,
      "note": "Reproduced the same TS2322 class recorded in tsc-rbg-red.log, then restored and re-confirmed green."
    },
    {
      "id": "secret-redaction",
      "command": "bun test src/orchestration/secret-scan.test.ts",
      "green": "5 pass / 0 fail / 8 expect() calls",
      "rbgGenuine": true,
      "note": "In-test RBG: planted fake sk-/AKIA/Bearer keys caught by scanForSecrets; redact() yields a clean re-scan. Independent grep over src/test/fixtures (excl. secret-scan.test.ts): zero key-shaped strings."
    },
    {
      "id": "hermetic-e2e-dry-run",
      "command": "bun test src/orchestration/e2e-dry-run.test.ts",
      "green": "3 pass / 0 fail / 15 expect() calls",
      "rbgGenuine": true,
      "note": "Doubles only — no net/mic/key imports. RBG cases inside the suite: red probe verdict halts the chain (feature never schedules/lands); incomplete bundle (missing green run) bounces at the serialized land lane."
    },
    {
      "id": "bun-test-full-suite",
      "command": "bun test ./src ./test (CI command)",
      "green": "42 pass / 0 fail / 159 expect() calls across 8 files (exit 0)",
      "rbgGenuine": "n/a-aggregate",
      "note": "CI (.github/workflows/ci.yml) runs `bun run typecheck` (tsc --noEmit) then `bun test ./src ./test`; both green."
    }
  ],
  "walkingSkeleton": {
    "touchesEverySeam": true,
    "seam": "transcript(loadFixture/replay) -> decision(match) -> trace(TraceProcessor LogEvent w/ non-empty correlationId)",
    "inProcessDoublesOnly": true,
    "noCueNetworkKeyImport": true,
    "imports": "node:crypto, node:fs, node:path, bun:test, type-only ./types.ts",
    "ci": ".github/workflows/ci.yml runs `bun run typecheck` + `bun test ./src ./test`"
  },
  "summary": "All blocking pre-merge gates re-run from disk on branch build/walking-skeleton-smoke and confirmed green. Smoke RBG reproduced live (green 1/1 with 12 expects; red is a real 1-assertion failure under BREAK_MATCHER=1, Expected 1 Received 0). tsc RBG reproduced live (real TS2322 on a planted src error, clean restore). secret-redaction 5/5 and hermetic e2e 3/3 pass with no net/mic/key imports. Full scoped suite `bun test ./src ./test` 42 pass/0 fail. Walking skeleton genuinely traverses transcript->decision->trace with in-process doubles only. PASS."
}

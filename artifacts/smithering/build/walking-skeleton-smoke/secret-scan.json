{
  "scannedScope": "src/**  test/**  fixtures/**",
  "excludedFiles": [
    "src/orchestration/secret-scan.test.ts"
  ],
  "excludeReason": "intentional fake key literals used to test the scanner itself (scanner RBG live in the test file); excluding them is documented here so reviewers can verify the exclusion is deliberate",
  "pattern": "sk-[A-Za-z0-9]{10,}|Bearer [A-Za-z0-9._-]{20,}|dg\\.[A-Za-z0-9]{10,}|AKIA[A-Z0-9]{16}",
  "hits": [],
  "verdict": "clean",
  "rbgRedRun": "evidence/secret-scan-rbg-red.log",
  "rbgGreenRun": "evidence/secret-scan-rbg-green.log",
  "note": "Red run shows scanner detects sk-* pattern in a synthetic temp file. Green run scans all product source (src/ test/ fixtures/) minus the intentional test-scanner data and finds zero hits."
}
